apiVersion: v1

kind: Pod

metadata:
  name: gpu-test
  namespace: inference

spec:

  restartPolicy: Never

  containers:

  - name: gpu-test

    image: nvidia/cuda:12.4.0-runtime-ubuntu22.04

    command:

      - nvidia-smi

    resources:

      limits:

        nvidia.com/gpu: 1